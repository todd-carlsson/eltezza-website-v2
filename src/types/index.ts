export type CarouselData = {
    id: string
    src: string
    title: string
    subTitle: string
    link: string
}

export type TeamData = {
    id: string
    name: string
    src: string
    position: string
}
